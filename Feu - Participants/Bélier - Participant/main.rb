require './verification'
allrubies = [] << allrubies = CSV.parse(File.read(rubies.txt), headers: true)
File.open(rubies.txt, w+)
file = open(file.txt, r)

> "ruby" 

def getrubies:
    line_num=0
text=File.open(rubies.txt).read
text.gsub!(/\r\n?/, \n)
text.each_line do |line|
    print "#{line_num += 1} #{line}"
end
